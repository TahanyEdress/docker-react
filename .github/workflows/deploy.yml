 
name: deploy react 
on:
 push:
  branches:
   - master
jobs:
 build_:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v2 
   - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
   - run: docker build -t tahanyyedress/docker-react -f Dockerfile.dev .
   - run: docker run -e CI=true tahanyyedress/docker-react npm run test
   - run: docker build -t tahanyedress/multi-client ./microservices/client
   - run: docker build -t tahanyedress/multi-nginx  ./microservices/nginx
   - run: docker build -t tahanyedress/multi-server ./microservices/server
   - run: docker build -t tahanyedress/multi-worker ./microservices/worker
   
   - run: docker push tahanyedress/multi-client 
   - run: docker push tahanyedress/multi-nginx
   - run: docker push tahanyedress/multi-server
   - run: docker push tahanyedress/multi-worker
   
    ## create artifcat for deployment
    #   - name: Create deployment package 
    #     run : zip -r deploy.zip . -x '*.git*'

    #   - name: Deploy to EB (Elastic Beanstalk)
    #     uses: einaregilsson/beanstalk-deploy@v22
    #     with:
    #      aws_access_key: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #      aws_secret_key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    #      application_name: "react"
    #      environment_name: "React-env"
    #      version_label: ${{github.sha}}
    #      region: "us-east-1"
    #      deployment_package: deploy.zip
